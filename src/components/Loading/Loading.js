import { FiLoader } from "react-icons/fi";
import "./style.css";

const Loading = () => {
  return (
    <div className="loading-wrapper">
      <FiLoader />
    </div>
  );
};

export default Loading;
